# Copy to .env.local (gitignored) and fill in. Neither value is a secret —
# both ship to the browser by design; access control is Firebase auth + CORS.
VITE_BACKEND_URL=
VITE_FIREBASE_CONFIG={"apiKey":"...","authDomain":"<project>.firebaseapp.com","projectId":"<project>","appId":"..."}
